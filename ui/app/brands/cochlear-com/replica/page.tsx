// The `homepage` route (/replica) and the `home` route (/replica/home) both
// target cochlear's home page. The model-built React home page is faithful, so
// render it here too instead of falling back to the unstyled HTML snapshot.
export { default } from "./home/page";
