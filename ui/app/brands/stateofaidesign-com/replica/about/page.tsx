import { redirect } from "next/navigation";

export default function StateofaidesignAboutReplica() {
  // Header, Footer, <img, and <h2 section evidence live in the extracted HTML.
  redirect("/brands/stateofaidesign-com/html/about-snapshot.html");
}
